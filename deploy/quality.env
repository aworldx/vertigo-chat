# Non-secret placeholders used only by `script/check-infrastructure` to parse Compose.
PHX_HOST=quality.local
PHX_SCHEME=https
PHX_URL_PORT=443
SECRET_KEY_BASE=quality-secret-key-base
POSTGRES_USER=quality
POSTGRES_PASSWORD=quality
POSTGRES_DB=quality
OPENAI_API_KEY=quality
METRICS_TOKEN=quality
METRICS_TOKEN_FILE=/dev/null
CADDY_SITE_ADDRESS=https://quality.local
GRAFANA_ADMIN_PASSWORD=quality
