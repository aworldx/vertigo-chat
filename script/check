#!/bin/sh
# Repository-wide quality gate. Service-specific checks remain available separately.
set -eu

cd "$(dirname "$0")/../apps/phoenix"
mix precommit
# Accounts browser checks already run inside precommit coverage.
exec ../../script/check-infrastructure
