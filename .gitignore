# Назначение файла: список локальных и сгенерированных файлов, которые не должны попадать в git.
# The directory Mix will write compiled artifacts to.
/apps/phoenix/_build/
# Legacy local artifacts from before Phoenix was moved; safe to remove locally.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/apps/phoenix/cover/
/cover/

# The directory Mix downloads your dependencies sources to.
/apps/phoenix/deps/
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/apps/phoenix/doc/
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ignore package tarball (built via "mix hex.build").
chat-*.tar

# Ignore assets that are produced by build tools.
/apps/phoenix/priv/static/assets/

# Ignore digested assets cache.
/apps/phoenix/priv/static/cache_manifest.json

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/apps/web/node_modules/
/apps/web/test-results/
/apps/web/coverage/
/coverage/

.env
.env.docker.local
.env.vps
.music_proxies
/logs/

# macOS Finder metadata.
.DS_Store

/apps/web/dist/

/apps/web/migration-results/

# Local authenticated music proxies (credentials).
/music-proxies.txt
